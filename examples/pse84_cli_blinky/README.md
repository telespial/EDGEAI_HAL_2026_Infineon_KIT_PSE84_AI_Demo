# PSE84 CLI Blinky

Minimal bring-up source for KIT_PSE84_AI.

## Notes
- This is source-only and intended to be integrated into a ModusToolbox app project.
- Uses `CYBSP_USER_LED1` and toggles every 500 ms.
- Keep this as a known-good hardware smoke test before larger app integration.
